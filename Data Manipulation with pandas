## nunique()user_id **counting each user only once**
> `nunique()` means **number of unique values**
<ins> user_count = orders_df['user_id'].nunique() </ins>
user_count
## the total 
<ins> trips_count = trips_df['trip_id'].count() </ins>
trips_count
